                
                ThumbnailSource = await BitmapTools.LoadBitmapAsync(source.Thumbnail)
                PictureSource = await BitmapTools.LoadBitmapAsync(source.Picture);

                    // Mod bitmap
                    item.ThumbnailSource = await BitmapTools.LoadBitmapAsync(item.Thumbnail);
                    item.PictureSource = await BitmapTools.LoadBitmapAsync(item.Picture);


                    // Mod bitmap
                    item.ThumbnailSource = await BitmapTools.LoadBitmapAsync(item.Thumbnail);
